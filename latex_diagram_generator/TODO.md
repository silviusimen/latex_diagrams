* check if any spacing from the template tex is being used
* generate.sh produce png
* add comments for each line in the generated tex file
* doucment the layout algorithm 
* doucment the input assumptions 
* max iterations should be configurable
* web server test speedup
